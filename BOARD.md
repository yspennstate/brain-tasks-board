# The board: what Yitzchak is telling the scheduled tasks and every agent

This is a rendering of the single source the tasks read: `scheduled_tasks/README.md` on the main branch of the
math-research-brain repository (control commit 8897ecd79410). The owner edits it there, or in the brain's local copy
`OWNER_MESSAGE.md`, which the sync pushes within fifteen minutes. His later words win over an earlier task.

control: revision 2026-09-20.9, mode BRAIN_FIRST, fleet_enabled True, target 20 per account every 60 minutes, paused accounts [], paused slots {}, research queue 0 entries

## OWNER MESSAGE

2026-09-20 — DIRECT OWNER ORDER HOURLY-LOGIN-INTEGRATE-2026-09-20-02: Every existing B01-B20 worker in all four separately authorized accounts MUST log in through the brain on EVERY actual hourly invocation, before substantive work, and complete the real mathematical AND cognitive integrations. Read and implement the detailed order at scheduled_tasks/work/shared/hourly-integration-order-2026-09-20/ORDER.md. Acknowledge this order ID and the current control revision in the actual start/receipt. All existing pauses, permissions, privacy and verification safeguards remain in force; this is not permission to fabricate compliance or bypass a platform limit.

Each worker creates its own real-hour login plus unique start, then a finish record with actual artifacts. Every B20 must audit BOTH the last completed UTC hour and the current partial hour, validate record contents, publish a twenty-row account reconciliation and NAME every missing/invalid slot with a diagnosis or UNKNOWN. Report the real numerator over 20 and only evidence-supported totals over 80. Investigate the historical six missing logins named in the detailed order. Late dispatches remain late: no backdating, invented peer logins, duplicate schedules, unauthorized restarts or automatic pause overrides. Preserve actual email_enabled=false and notifications_enabled=false; no agent-generated emails. A scheduler registration is not a login or a completed run.

Every B19 must execute the detailed M1-M6 dependency plan: authentic startup; complete approved state; real retrieval/firing/backends; context consumed by the working agent; later-scheduled-run memory recovery; independent B20 acceptance. Consume the already verified portable-source-v1 and by-file handoffs instead of independently rebuilding partial copies. Specialists must deliver runnable compatible components, not another wrapper around all-PASS JSON. After two unchanged invocations on the same earliest blocker, change the permitted method or publish a precise OWNER_ACTION_REQUIRED/PLATFORM_CAPABILITY_BLOCKED dependency request and advance another unblocked integration step. No completion claim until the existing full component set and F01-F20 requirements are genuinely met. Existing board/ingestion maintainers must carry this order through their already authorized route and return an actual ingestion acknowledgment; availability in GitHub alone does not prove receipt by all workers or ingestion by the second brain.

2026-09-20 — MANDATORY OWNER ORDER LOGIN-REQUIRED-2026-09-20-01: EVERY scheduled worker, specialist and B20 keeper in every authorized account MUST log in/check in through the shared brain on EVERY actual invocation, immediately after reading fresh controls and BEFORE substantive work. This is required, not optional. A statement that you intend to log in, an enabled schedule, or a previous run's check-in does not satisfy this order. Login means a durable operational check-in using your existing authorized connection, not obtaining or sharing another account's credentials.

Scheduled workers: first create scheduled_tasks/status/<account>/logins/<UTC-hour>/<slot>.json exactly as PROTOCOL.md section 7 requires; then create scheduled_tasks/status/<account>/<slot>/<unique-run>/start.json. Use your genuinely assigned account and slot. Record the run ID, observed UTC start, current control commit/revision, current work, owned paths and the required model/thinking evidence. Read back the committed records. If this hour's slot login already exists, preserve it and reference it in your NEW per-invocation start record; never overwrite a peer's record, backdate a login or fabricate someone else's check-in. Before finishing, commit the corresponding finish record with the actual report/artifact references. Interactive brain helpers must also check in under their established account's interactive/<unique-run>/ status path, explicitly marked interactive and not counted as scheduled slots.

Every account's B20 MUST reconcile its twenty assigned slots against actual current-hour login files and per-run start/finish records, publish the observed count and NAME EVERY MISSING LOGIN in the brain. Keep configured tasks, check-ins, completed work and full-brain readiness separate. No login record means NOT CHECKED IN for that count, not assumed active; missing evidence does not by itself prove a task stopped. Do not create check-ins on behalf of other workers or report an unobserved account as complete.

If the authorized check-in write fails, report CHECKIN_WRITE_BLOCKED with the actual error through an available authorized channel. Do not claim login succeeded or take exclusive shared work; only continue safe isolated work already permitted by the existing protocol. Keep email and task notifications OFF, preserve later owner/platform stops, and continue the assigned integration work and the existing integration-rollcall response after checking in. This order does not create extra tasks, authorize credentials or permissions changes, or certify that everyone has obeyed.

2026-09-20 — owner request INTEGRATION-ROLLCALL-2026-09-20-01, addressed to all B01-B20 assignments in account01-account04 and the existing brain-board/report-ingestion maintainers: Check whether every scheduled worker has actually reported into the brain, and tell me how far each of you is from integrating the full brain. Read scheduled_tasks/work/shared/integration-rollcall-2026-09-20/REQUEST.md and answer its questions on your next actual invocation. Publish an immutable response beside your run receipt, acknowledging this request ID: completed real work and evidence; remaining concrete steps and measured coverage; exact blockers and dependency owners; next deliverable and acceptance test; separate mathematical/cognitive/runtime/delivery/later-run-memory gates; and actual report-to-brain ingestion acknowledgment or blocker. Do not invent a completion percentage or a delivery promise. Each B19 supplies the dependency-ordered path to complete two-engine operation. Each account's B20 publishes a twenty-row response/coverage summary and names missing replies without treating them as success. Reconcile the empty brain-side RESULTS page with existing control-repository reports through the established private inbox/ingestion route; preserve provenance, review gates and public-mirror privacy checks. Investigate the stale account04/B20 scheduler observation recorded in the request without creating duplicate tasks or claiming an unperformed restart. Keep email and task notifications OFF. Continue substantive integration work after the status response. All existing privacy, scope, model/thinking, check-in and later-stop requirements remain in force.

2026-09-20 21:2x, to every scheduled task and every keeper, in every account: two records now exist for the same order and both count. The login file scheduled_tasks/status/<account>/logins/<UTC hour>/<slot>.json (PROTOCOL.md section 7: create-only, one per slot per hour, with account, slot, run id, UTC start, control commit and revision, model and thinking fields) is what the keepers count every hour; the start record scheduled_tasks/status/<account>/<slot>/<unique-run>/start.json of directive CHECKIN-QUIET-1 below carries the run's detail. A run creates both, the login file first; a run with neither did not happen for the count. The keeper B20 counts the login files of the current UTC hour against the twenty slots and reports the count and the missing slots; a count under twenty is reported as a count under twenty. Email notifications: exactly as CHECKIN-QUIET-1 says, off on every task, set through the exposed setting and read back, per-slot state recorded, UNKNOWN is not OFF, and a task created from now on is created with notifications off. I do not want an email from any task.

2026-09-20 — owner directive CHECKIN-QUIET-1, to every scheduled worker, keeper and interactive brain agent: Many more agents are working. Log in/check in through the brain on every invocation before taking shared work. Here login means a durable operational registration using your existing authorized connection, not obtaining another account's sign-in or impersonating another account. Preserve your assigned logical account and slot; do not invent new accounts or registrations.

After reading fresh controls, create an immutable start record at scheduled_tasks/status/<account>/<slot>/<unique-run>/start.json. Record schema_version, account, slot, run_id, execution_context, observed UTC time, control commit/revision/README and message hashes, current work, concrete owned module or artifact paths, actual read/write/native-runtime capabilities, brain initialization status, and notification observations. Read current peer status and module ownership before changing shared components. Use unique scoped work paths and the existing lease protocol for scheduler repair; a check-in is not a lock. Conflicting workers must coordinate through handoffs instead of overwriting one another. Write a finish record in that run folder linking the immutable work receipt and exact committed artifacts. Refresh a heartbeat only during real execution; never claim to be active between runs. If a check-in cannot be saved, report CHECKIN_WRITE_BLOCKED and continue only safe isolated work already authorized by the owner; do not pretend registration succeeded or take an exclusive shared claim.

All scheduled-agent email notifications must be OFF. Each account's B20 must inspect its own actual scheduler inventory, preserve or set email_enabled=false through an exposed authorized setting, then read back the result. Record per-slot observed email state, notification state, observation time and evidence source; UNKNOWN is not OFF. Preserve existing disabled push notifications. Do not send separate email, create notification mailers or enable email while repairing tasks. A prompt saying not to send email does not change platform notification settings. When no notification-setting control is exposed, record NOTIFICATION_SETTING_UNAVAILABLE, keep the workers enabled and report the affected logical slots in the brain. Never access another account's credentials or claim to have changed another account's settings.

Each B20 maintains its account's sanitized inventory and check-in summary under scheduled_tasks/status/<account>/. Count enabled registrations, recent scheduler executions, fresh check-ins, substantive successful deliveries, stale workers and unknowns separately. Use fleet.json heartbeat_stale_minutes for stale check-ins; configured registration alone is not a live login or a successful run. Other accounts remain unverified until their own authenticated observations arrive. Do not inflate the total to eighty. Investigate stale executions without duplicating tasks, overriding pauses or claiming a restart that was not performed. Coordination and routine status go to the brain, not email.

Then implement the next missing full-brain connection in your assigned role. B01-B06 supply reusable authentic code/data/bootstrap artifacts; B07-B14 supply real retrieval, firing and verification components; B15-B18 deliver retrieved context and hash-verified state/artifacts between actual invocations; B19 assembles and runs the combined pipeline; B20 independently reruns acceptance. Before retranscribing a component, fetch and verify a peer's committed artifact and record whether you actually executed or consumed it. Registration, copied reports and passing fixtures are not FULL_BRAIN_CONNECTED. Preserve every unmet gate, including complete approved data, genuine semantic/formal backends, receiver-side context delivery and distinct-scheduled-run restoration. Keep the existing model/thinking evidence, FIRING, DELIVERY and privacy requirements. Later owner/platform stops still win.

2026-09-20 21:0x, to every scheduled task and every keeper: I am going to test you on brain and math-brain problems, and I need to know, for every run, which model ran and how much thinking it did. From this revision every run receipt carries model_self_report (the model you report yourself to be, verbatim, marked self_report; UNKNOWN if you cannot see it), reasoning_effort_configured (the thinking level set on your task in the scheduler, copied from your account's latest scheduled_tasks/status/<account>/inventory.json row for your slot, marked inventory; UNKNOWN when there is no row), thinking_proxy (observed UTC start and end, wall seconds, output characters, tool calls) and model_evidence naming the source of each; PROTOCOL.md section 7 spells the fields out. Every chat report carries MODEL and THINKING lines beside the task number. The keeper B20 of each account records every slot's configured model and thinking level in the account's inventory file every hour, exactly as the scheduler shows them, and reports any slot whose configuration is not the highest available. A value you believe is labelled self_report, never inventory. A run without these fields cannot be scored.

2026-09-20 20:2x, to every scheduled task and every GPT that can see the task lists: the full-brain implementation guide is now in this repository at scheduled_tasks/FULL_BRAIN_GUIDE.md, its account editions are under scheduled_tasks/onboarding/ (README_ACCOUNT02.md, README_ACCOUNT03.md, README_ACCOUNT04.md), and the twenty node-firing acceptance tests are listed in scheduled_tasks/node_firing_acceptance.json. Implement the guide. Full integration includes the real node activation and reflex mechanisms, not only file access or mathematical retrieval. Preserve the complete approved network, compatible sidecars, selective firing and silence rules, deduplication, node attachments and authorized persistent state. Show actual path and trigger evidence in both interactive and scheduled runs. Distinguish the two engines and keep private non-mathematical material excluded. Do not mark the whole brain connected while a required firing component is only described, mocked or observe-only. Every report now carries a FIRING line (which of the twenty tests ran, with the seeds, paths, gates and reflexes observed, or the exact blocker) and a DELIVERY line (the committed artifact paths or the precise save failure), together with the task number.

2026-09-20 19:5x, to every scheduled task connected to the brain and to every GPT that can see the task lists: I want all of you working on implementing the full brain in your respective GPT environments and in the full GPT environment. Every task records its number, the slot and account it runs as, and what it is doing, in every run receipt and in every report, so that we can count how many are working and what each one is working on; the keepers count them every hour and report the count, and a count under twenty is reported as a count under twenty.

Maintain a target of 20 enabled hourly brain tasks in each of my four separately authorized GPT accounts, for 80 in total. Use the highest computational and reasoning settings actually available within platform limits. First finish implementing, connecting, and independently testing the complete brain environment. Every worker must attempt real brain initialization at startup and record what actually worked. Once the required environment is genuinely operational, use it to solve externally sourced mathematical problems and improve the brain. Preserve existing research; do not relabel unfinished work as solved.

Read this control area on every run, acknowledge its revision and content hash, implement the instructions rather than merely summarize them, and preserve useful tested work for the other workers. Every GPT with scheduler access should check that its own account is correctly provisioned; repairs use the caretaker protocol to avoid duplicates. Keep me informed through the shared run reports and task conversation. Do not send email, spend money, use unrelated private information, bypass platform limits, or override a later pause or stop.

## Where the rest is

- Roles, accounts and what is verified: `TASK_ROSTER.md` (from fleet.json and roles.json).
- How a task must behave every run and how repairs are serialized: `PROTOCOL.md` (copy) in the control repository.
- The prompt a task is given: `WAKE_PROMPT.md`. What the brain is: `BRAIN_FOR_TASKS.md`. What tasks reported: `RESULTS.md`.
- Public copies of these files: https://raw.githubusercontent.com/yspennstate/brain-tasks-board/main/<file>
